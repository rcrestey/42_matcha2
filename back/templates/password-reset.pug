extends _layout.pug

append style
    style.
        h2.welcome {
            background: white;
            padding: 20px 20px;
            margin: 0;
            text-align: center;
            background: none;
            margin: 0;
        }
        .confirm-button {
            background: #2B6CB0;
        }

block head
    h2.welcome.
        Password reset

block content
    row
        column(style={ 'padding': '10px 20px' })
            p
                | Follow the link to reset your password
    row
        column(style={ 'padding-bottom': '20px' })
            button.confirm-button(href=reset_password_link) Reset my password
